export interface Project {
    id: string;
    title: string;
    category: string;
    color: string;
}

export interface Post {
    id: string;
    title: string;
    date: string;
    preview: string;
}
